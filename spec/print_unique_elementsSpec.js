describe('UniqueElement', function () {
    it('Null element is not unique', function () {
        expect(isUniqueElement(null)).toBe(true)
    })

    it('Null element is unique', function () {
        expect(isUniqueElement(null)).toBe(true)
    })


})

